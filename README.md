# Carrowmont main website — clean root build v2.0.0

This package is intended to replace the active files in the root `Draw004/draw004.github.io` GitHub Pages repository.

Active shared files:
- `carrowmont.css`
- `locale.js`
- `site.js`
- `journey.svg`

The HTML pages reference only those shared files. Legacy `home*.css`, `home*.js`, and older hero image files are not used.
